# Documentation

## Design

- [Persistent World Game](design-game-persistent-world.md) — Web-based game with 7-day simulation, 5×5 grid, buildings (levels 1–10), and coins.

## Configuration

- [HTTP auth (httpauthshim)](auth-httpauthshim.md) — Auth config under root key `auth`, config file `~/.config/wf/config.yaml`.
