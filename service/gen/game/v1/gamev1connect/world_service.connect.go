// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: game/v1/world_service.proto

package gamev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
	v1 "webfarm/gen/game/v1"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// WorldServiceName is the fully-qualified name of the WorldService service.
	WorldServiceName = "game.v1.WorldService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// WorldServiceInitProcedure is the fully-qualified name of the WorldService's Init RPC.
	WorldServiceInitProcedure = "/game.v1.WorldService/Init"
	// WorldServiceGetWorldStateProcedure is the fully-qualified name of the WorldService's
	// GetWorldState RPC.
	WorldServiceGetWorldStateProcedure = "/game.v1.WorldService/GetWorldState"
)

// WorldServiceClient is a client for the game.v1.WorldService service.
type WorldServiceClient interface {
	// Init returns bootstrap data (e.g. configured auth providers). Call on page load.
	Init(context.Context, *connect.Request[v1.InitRequest]) (*connect.Response[v1.InitResponse], error)
	// GetWorldState returns current day, cycle, and next tick time.
	GetWorldState(context.Context, *connect.Request[v1.GetWorldStateRequest]) (*connect.Response[v1.WorldState], error)
}

// NewWorldServiceClient constructs a client for the game.v1.WorldService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewWorldServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) WorldServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	worldServiceMethods := v1.File_game_v1_world_service_proto.Services().ByName("WorldService").Methods()
	return &worldServiceClient{
		init: connect.NewClient[v1.InitRequest, v1.InitResponse](
			httpClient,
			baseURL+WorldServiceInitProcedure,
			connect.WithSchema(worldServiceMethods.ByName("Init")),
			connect.WithClientOptions(opts...),
		),
		getWorldState: connect.NewClient[v1.GetWorldStateRequest, v1.WorldState](
			httpClient,
			baseURL+WorldServiceGetWorldStateProcedure,
			connect.WithSchema(worldServiceMethods.ByName("GetWorldState")),
			connect.WithClientOptions(opts...),
		),
	}
}

// worldServiceClient implements WorldServiceClient.
type worldServiceClient struct {
	init          *connect.Client[v1.InitRequest, v1.InitResponse]
	getWorldState *connect.Client[v1.GetWorldStateRequest, v1.WorldState]
}

// Init calls game.v1.WorldService.Init.
func (c *worldServiceClient) Init(ctx context.Context, req *connect.Request[v1.InitRequest]) (*connect.Response[v1.InitResponse], error) {
	return c.init.CallUnary(ctx, req)
}

// GetWorldState calls game.v1.WorldService.GetWorldState.
func (c *worldServiceClient) GetWorldState(ctx context.Context, req *connect.Request[v1.GetWorldStateRequest]) (*connect.Response[v1.WorldState], error) {
	return c.getWorldState.CallUnary(ctx, req)
}

// WorldServiceHandler is an implementation of the game.v1.WorldService service.
type WorldServiceHandler interface {
	// Init returns bootstrap data (e.g. configured auth providers). Call on page load.
	Init(context.Context, *connect.Request[v1.InitRequest]) (*connect.Response[v1.InitResponse], error)
	// GetWorldState returns current day, cycle, and next tick time.
	GetWorldState(context.Context, *connect.Request[v1.GetWorldStateRequest]) (*connect.Response[v1.WorldState], error)
}

// NewWorldServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewWorldServiceHandler(svc WorldServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	worldServiceMethods := v1.File_game_v1_world_service_proto.Services().ByName("WorldService").Methods()
	worldServiceInitHandler := connect.NewUnaryHandler(
		WorldServiceInitProcedure,
		svc.Init,
		connect.WithSchema(worldServiceMethods.ByName("Init")),
		connect.WithHandlerOptions(opts...),
	)
	worldServiceGetWorldStateHandler := connect.NewUnaryHandler(
		WorldServiceGetWorldStateProcedure,
		svc.GetWorldState,
		connect.WithSchema(worldServiceMethods.ByName("GetWorldState")),
		connect.WithHandlerOptions(opts...),
	)
	return "/game.v1.WorldService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case WorldServiceInitProcedure:
			worldServiceInitHandler.ServeHTTP(w, r)
		case WorldServiceGetWorldStateProcedure:
			worldServiceGetWorldStateHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedWorldServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedWorldServiceHandler struct{}

func (UnimplementedWorldServiceHandler) Init(context.Context, *connect.Request[v1.InitRequest]) (*connect.Response[v1.InitResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("game.v1.WorldService.Init is not implemented"))
}

func (UnimplementedWorldServiceHandler) GetWorldState(context.Context, *connect.Request[v1.GetWorldStateRequest]) (*connect.Response[v1.WorldState], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("game.v1.WorldService.GetWorldState is not implemented"))
}
